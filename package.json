{
	"name": "reliatools",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"vercel-build": "npx @cloudflare/next-on-pages && next build"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"vitest": "~3.0.7",
		"wrangler": "^4.14.1"
	},
	"dependencies": {
		"next": "^15.3.1",
		"react": "^19.1.0",
		"react-dom": "^19.1.0"
	}
}
